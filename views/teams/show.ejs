<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Team Page</title>
</head>

<body>
    <%- include('../nav/_nav.ejs') %>
    <h1><%= team.name %></h1>
    <h3>City: <%= team.city %></h3>
    <h3>Mascot: <%= team.mascot %></h3>
    <h3>Color: <%= team.color %></h3>
    <h3>Year Founded: <%= team.yearFounded %></h3>
    <h3>Status: <%= team.status %></h3>
    <form action="/users/<%= user._id %>/teams/<%= team._id %>?_method=DELETE" method="POST">
        <button type="submit">Delete Team</button>
    </form>
    <form action="/users/<%= user._id %>/teams/<%= team._id %>/edit" method="GET">
        <button type="submit">Edit Team</button>
    </form>
    <h2>Players</h2>
    <form action="/users/<%= user._id %>/players/new" method="GET">
        <button type="submit">Add New Player</button>
    </form>
</body>
</body>

</html>