<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Player</title>
</head>

<body>
    <%- include('../nav/_nav.ejs') %>
        <h1>Edit Player</h1>
    <form action="/users/<%= user._id %>/players/<%= player._id %>?_method=PUT" method="POST">
    </form>
    <form action="/users/<%= user._id %>/players" method="POST">
        <label for="name">Name:</label>
        <input type="text" name="name" id="name" value="<%= player.name %>" required>
        <br>
        <br>
        <label for="position">Position:</label>
        <input type="text" name="position" id="position" value="<%= player.position %>" required>
        <br>
        <br>
        <label for="number">Number:</label>
        <input type="number" name="number" id="number" value="<%= player.number %>" required>
        <br>
        <br>
        <label for="height">Height:</label>
        <input type="text" name="height" id="height" value="<%= player.height %>" required>
        <br>
        <br>
        <label for="weight">Weight:</label>
        <input type="text" name="weight" id="weight" value="<%= player.weight %>" required>
        <br>
        <br>
        <label for="status">Status:</label>
        <select name="status" id="status" required>
            <option value="active" <% if(player.status === 'active') { %> selected <% } %>>Active</option>
            <option value="inactive" <% if(player.status === 'inactive') { %> selected <% } %>>Inactive</option>
        </select>
        <br>
        <br>
        <button type="submit">Update player</button>
    </form>
</body>

</html>